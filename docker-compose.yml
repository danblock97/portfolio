name: portfolio

services:
  web:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio:latest
    restart: unless-stopped
    expose:
      - "80"
    ports:
      - "${HOST_PORT:-3000}:80"
