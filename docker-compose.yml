services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    image: portfolio-webpage:local
    environment:
      - NODE_ENV=production
    # Do not publish host ports here; Dokploy will route internally
    expose:
      - "80"
    restart: unless-stopped
